{
  "name": "@zax/cli",
  "version": "0.1.0",
  "license": "MPL-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'CLI runs via bun directly, no bundling needed'",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0",
    "typescript": "^5.0.0"
  }
}
